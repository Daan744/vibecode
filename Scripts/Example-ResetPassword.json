{
  "name": "Reset User Password",
  "description": "Resets the password for a specified user via Microsoft Graph PowerShell. Requires the Microsoft.Graph module.",
  "scriptFile": "Example-ResetPassword.ps1",
  "requiredModules": ["Microsoft.Graph.Users.Actions"],
  "requiredPermissions": ["User.ReadWrite.All"],
  "parameters": [
    {
      "name": "UserPrincipalName",
      "displayName": "User Principal Name",
      "type": "string",
      "required": true,
      "default": "",
      "description": "The UPN of the user (e.g. john@contoso.com)"
    },
    {
      "name": "NewPassword",
      "displayName": "New Password",
      "type": "string",
      "required": true,
      "default": "",
      "description": "The new password to set"
    },
    {
      "name": "ForceChangeOnLogin",
      "displayName": "Force change on next login",
      "type": "choice",
      "required": false,
      "default": "true",
      "choices": ["true", "false"],
      "description": "Whether the user must change password at next sign-in"
    }
  ]
}
